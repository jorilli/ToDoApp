<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ToDoApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ToDoApp"
    Shell.FlyoutBehavior="Disabled"
    Shell.NavBarIsVisible="False"
    Shell.TabBarBackgroundColor="#dcdede"
    Shell.TabBarForegroundColor="Black">

    <TabBar>
        <Tab Icon="home.png">
            <ShellContent ContentTemplate="{DataTemplate local:MainPage}"/>
        </Tab>
        <Tab Icon="profil.png">
            <ShellContent ContentTemplate="{DataTemplate local:Account}"/>
        </Tab>
        <Tab Icon="logo_kurz.png">
            <ShellContent ContentTemplate="{DataTemplate local:InfoPage}"/>
        </Tab>
        <Tab Icon="exit.png">
            <ShellContent ContentTemplate="{DataTemplate local:Exit}"/>
        </Tab>
    </TabBar>

</Shell>
